apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
metadata:
  name: istio-mesh
spec:
  values:
    global:
      meshID: mesh1
      multiCluster:
        clusterName: test
      network: network1
